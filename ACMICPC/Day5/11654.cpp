#include <iostream>
using namespace std;
int main(void) {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    char input;
    cin >> input;
    cout << (int)input;
    return 0;
}