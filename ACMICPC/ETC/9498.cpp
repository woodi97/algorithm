#include <iostream>

int main(void){
    int sc;
    std::cin >> sc;

    if(sc>=90&&sc<=100)
        std::cout << "A";
    else if(sc>=80&&sc<90)
        std::cout << "B";
    else if(sc)

    return 0;
}