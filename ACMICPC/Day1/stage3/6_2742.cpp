#include <iostream>
using namespace std;

int main(void)
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int tc = 0;
    cin >> tc;
    for (int i = tc; i > 0; i--)
    {
        cout << i << '\n';
    }
    return 0;
}